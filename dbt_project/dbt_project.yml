name: 'retail_vault'
version: '1.0.0'
config-version: 2

# Ссылка на профиль (подключение), который мы опишем ниже
profile: 'retail_vault_profile'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analysis"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages", "logs"]

models:
  retail_vault:
    staging:
      +materialized: view
      +schema: staging
    raw_vault:
      +materialized: incremental
      +schema: raw_vault
    marts:
      +materialized: table
      +schema: marts